pipeline {
    agent any
    /* {
        docker {
            images { 'node:lts-buster-slim' 

                        
            args '-p 3000:3000' 
        }
    }*/
     environment {
        CI = 'true'
    }
    stages {
        stage('Build') { 
            agent{
                docker {
                    image 'node:lts-buster-slim'
                }
            }
            steps {
               
                sh 'npm install'
                sh 'npm run build'
                
            }
        }
        stage('Test') {
            agent{
                docker {
                    image 'python'
                }
            }
            steps {
                sh 'pip3 install --upgrade pip'
                sh 'pip3 install  virtualenv'
                sh  'virtualenv /home/venv'
                sh  'source /home/venv/bin/activate'
                sh  'pip install selenium'
                sh  'python reacttest.py'
            }
        }  
        
        
        
    }       
}
